"use client";
import { DesignMetalHistory } from "@/app/app/design-metal/DesignMetalHistory";

export function DesignMetalHistoryWrapper(_props: { currentSector?: string }) {
  return <DesignMetalHistory />;
}
